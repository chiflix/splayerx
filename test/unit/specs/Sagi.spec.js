/*
// Don't support the testing for now


import helpers from '@/helpers';

describe('Sagi.healthCheck', function () {
  it('Testing Sagi.healthCheck', function () {
    helpers.methods.sagi().healthCheck((_err, response) => {
      if (response) {
        expect(response.getStatus()).to.contain(1);
      }
    });
  });
});

*/